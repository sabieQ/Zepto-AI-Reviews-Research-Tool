# Spike: Apple App Store (Zepto)

Date: 2026-07-25  
Reviewer: eng spike script  
Official docs / ToS links:

- App listing: https://apps.apple.com/app/id1575323645  
- Node lib: https://www.npmjs.com/package/app-store-scraper  
- Official: https://developer.apple.com/app-store-connect/api/  

Access obtained? **n/a** for unofficial; **pending** for App Store Connect  
Methods tested:

1. Apple public customer-reviews RSS → **0 entries** for Zepto (`in`/`us`/`gb`)  
2. Python `app-store-scraper==0.3.5` → **broken** (JSON parse / empty)  
3. Node `app-store-scraper` → **intermittent**: returned **50** reviews once (`in`), later runs returned **0** for Zepto and even Pokémon GO  

## Schema mapping (when Node succeeds)

| Our field | Lib field |
|-----------|-----------|
| content | `title` + `text` |
| author | `userName` |
| rating | `score` |
| posted_at | `updated` |
| url | `url` |
| external_id | `as-{id}` |
| source | `app_store` |

## Results

- Unreliable for automation today  
- Helper kept: `scripts/spikes/fetch_app_store_reviews.js`  
- Do not block Play pilot on iOS  

## Scores

| L | T | S | C | V | P |
|---|---|---|---|---|---|
| 2 | 2 | 1 | 5 | 4 | 3 |

## Verdict

**Conditional / fragile** — OK to retry in spikes; **prefer App Store Connect API** for 7b if team has access.  
If no Connect access: keep Node scraper as best-effort with strong retries + accept empty runs.

Legal review required? **Yes before production collector**

## Recommendation for 7b

1. Ask Apple Developer admin for App Store Connect API key.  
2. Until then, optional best-effort Node collector; do not treat empty pulls as hard failures of the whole pipeline.
